/**
 * Minimal emulator seed for Sales Portal BDD.
 * Extend with sales-portal test users in users/{uid}.appAccess when auth BDD is added.
 */
console.log("Sales Portal emulator seed complete (no-op baseline).");
